version: "3.8"

services:
  auto-halo:
    build: .
    ports:
      - "48008:8808"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///data/auto-halo.db
    restart: unless-stopped